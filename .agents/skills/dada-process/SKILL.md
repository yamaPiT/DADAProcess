---
name: dada-process
description: ソフトウェアの新規開発・機能追加・修正・バグ対応の依頼を受けた時に必ず使用します。要求定義・総合テスト仕様・設計・実装の全工程を、DADAプロセス（Phase 0〜5）に基づきオーケストレーションする標準開発ワークフローです。
---

# DADAプロセス 標準開発ワークフロー

本スキルは、DADAプロセス全体の進行を制御するオーケストレーターです。各Phaseでは、指定されたペルソナスキル（`.agents/skills/` 配下）を読み込んで作業を実行します。

**工程順序（テストファースト）**: Phase 1 要求定義 → **Phase 2 総合テスト仕様** → **Phase 3 アーキテクチャ設計** → Phase 4 実装 → Phase 5 評価。
テスト仕様を設計より先に作るのは、「テストが書けない要求＝曖昧な要求」を設計・実装に流す前にPhase 1へ差し戻し、手戻りを最小化するためである。

## 起動時の状態判定（毎回必ず実行）

新しい会話でこのスキルを開始したら、**作業を始める前に**以下の判定を上から順に行い、現在地を宣言すること。

1. `docs/process/last_phase_summary.md` を読む。
2. 「次フェーズ」が具体的に記載されている場合 → **そのPhaseから再開**する。「承認済みドキュメント一覧」に記載されたファイルを読み込み、文脈を復元する。
3. 記載が空欄・テンプレートのままの場合 → **新規開発**である。Phase 0 から開始する。
4. 再開か新規かを判定し、動作モード（次節参照）と合わせて、必ず最初の応答で以下の形式で宣言する。

   > 現在の状態: **[Phase X から再開 / 新規開発]**（根拠: last_phase_summary.md の記載）
   > 動作モード: **[モードA: オーケストレーター常駐型 / モードB: 会話リレー型]**（根拠: サブエージェント起動ツールの有無）

## フェーズ実行の方式（環境による切り替え）

実行環境によってアテンション・リセットの実現方法が異なる。**起動時の状態判定と同時に、自分の使用可能なツール一覧にサブエージェント起動ツール（`invoke_subagent` 等）があるかを確認し、動作モードを決めて宣言すること。**

| 実行環境 | サブエージェント | 動作モード |
| :--- | :--- | :--- |
| Antigravity 2.0 / Antigravity CLI | あり | **モードA: オーケストレーター常駐型** |
| Antigravity IDE / Antigravity v1 | なし | **モードB: 会話リレー型** |

製品名で判別できない場合は、ツールの有無を唯一の判定基準とする。

### モードA: オーケストレーター常駐型（Antigravity 2.0 / CLI）
- 各Phaseの作業を**独立したコンテキストを持つサブエージェントに委譲**する。メイン会話はオーケストレーターとして「Phase間の遷移・承認ゲートでのProduct Ownerとのやり取り」だけを扱い、全Phaseを通して存続する。
- サブエージェントへの指示に含めてよいのは、**(a) 適用するペルソナスキルのパス (b) `last_phase_summary.md` を最初に読む指示 (c) 入力となる承認済みドキュメントのパス (d) 成果物の出力先パス (e) Product Ownerからの明示的な指示** のみ。
- **メイン会話のチャット履歴・未確定の議論をサブエージェントへ要約して渡すことを禁止**する（承認済みドキュメントだけが真実である）。

### モードB: 会話リレー型（Antigravity IDE / v1）
- 1つの会話が**現在のPhaseの作業を直接実行**する（ペルソナスキルを自分に適用する）。
- 承認ゲート通過後、`context-reset` スキルの方式B（手動リセット）でProduct Ownerに新しいチャットウィンドウの開始を依頼し、会話を終える。
- 次の会話のエージェントが「起動時の状態判定」により `last_phase_summary.md` から文脈を復元し、次Phaseを引き継ぐ（会話のリレーでプロセス全体を構成する）。

## 共通原則
- **プロセスの遵守**: 基本原則（承認ゲート、アテンション-リセット、スケール制御、ID体系、承認の定義等）はすべてワークスペースルール `.agents/AGENTS.md` に従うこと。
- **ドキュメント準拠性**: 基本は `docs/guidelines/dada_document_guidelines.md` に従う。ただしProduct Ownerから「IEEE29148_2018に準拠」等の規格指定や、「企業のテンプレートに従う」等の指示があった場合は、`docs/templates/` 配下の該当ひな形ファイルを読み込み、その構造を絶対的に優先すること。
- **進行状況の常時表示**: DADAプロセス実行中は、**すべての応答の末尾に**以下の1行を表示し、迷子を防ぐこと。

  `[DADA | Phase X: フェーズ名 | 状態: 作成中/レビュー中/承認待ち | モードA/B]`

- **想定外の状況のエスカレーション**: 本スキルとAGENTS.mdのルールで判断できない状況に遭遇した場合、推測で進めず、作業を停止して状況と選択肢をProduct Ownerに報告すること。

## 禁止事項（オーケストレーション全体）
- **フェーズのスキップ禁止**: スケール判定（Phase 0）で省略が認められたPhase以外を飛ばしてはならない。
- **先行作業の禁止**: 承認ゲート通過前に、次Phaseの成果物（特にソースコード）を作り始めてはならない。「ついでに実装しておきました」は重大なプロセス違反である。
- **複数Phaseの一括実行禁止**: 1回の応答で複数の承認ゲートをまたいではならない。各ゲートで必ず停止し、Product Ownerの入力を待つこと。
- **承認の推定禁止**: `.agents/AGENTS.md` の「承認の定義」に該当しない発言を承認と見なしてはならない。

## 共通レビュー手順
Phase 1〜3で繰り返す標準手順。各Phaseでは以下のパラメータを指定して実行する。

| パラメータ | 説明 |
| :--- | :--- |
| **作成スキル** | 文書を作成・修正するペルソナスキル |
| **レビュースキル** | 文書を検証するペルソナスキル |
| **成果文書** | 出力する開発ドキュメントのパス |
| **レビュー記録** | 指摘と対応を記録するレビュー記録表のパス |

1. **[作成]**: 作成スキルを読み込んでペルソナを適用し、成果文書を作成する。
2. **[レビュー]**: レビュースキルを読み込んでペルソナを適用。指摘をレビュー記録に記録する。
3. **[修正・記録]**: 作成スキルのペルソナに戻り、指摘を修正してレビュー記録に対応結果を記録する。（レビュー→修正は最大2周。**2周目のレビューは全文を見直さず、各レビュアースキルの「2周目以降の再レビュー」に従いOpen指摘と変更箇所だけを対象とする。**残ったHigh指摘は隠さず次項で報告する）
4. **[承認ゲート]**: 以下の定型で報告し、Product Ownerの承認を待つ。**承認が得られるまで次の作業を一切行わない。**

   > Phase X（フェーズ名）の成果物が完成しました。
   > - 成果文書: [パス]
   > - 自己レビュー: 指摘N件（うち解消M件、残課題K件）→ 詳細は [レビュー記録のパス]
   > - 残課題・TBD: [なし / 内容]
   > ご確認のうえ、承認いただけますか？（修正点があれば、チャットまたはREV101への記入でご指摘ください）

5. **[リセット]**: 承認後、スキル `context-reset` を実行し、次のPhaseへ遷移する。

---

## Phase 0: スケール判定
1. Product Ownerからの指示内容を分析し、`.agents/AGENTS.md` の「スケール制御」に基づき変更スケール（Hotfix / Minor / Major）を判定する。
2. 判定結果を「本変更は **[スケール]** と判定しました。理由: ～。適用フロー: ～」の形式で報告する。
3. 判定に応じたフローで以降のPhaseを実行する。**判定に迷う場合は必ずMajor（全Phase実行）を選び、Product Ownerに確認する。**

## Phase 1: 要求定義 (Requirements Definition)
共通レビュー手順を以下のパラメータで実行する。

| パラメータ | 値 |
| :--- | :--- |
| 作成スキル | `.agents/skills/requirements-engineer/SKILL.md` |
| レビュースキル | `.agents/skills/requirements-reviewer/SKILL.md` |
| 成果文書 | `docs/artifacts/SW105_ソフトウェア要求仕様書.md` |
| レビュー記録 | `docs/artifacts/REV101_Requirements.md` |

**完了条件**: すべての機能要求に `REQ-ID` と検証条件（Acceptance Criteria）が付与され、Product Ownerの承認を得ていること。

## Phase 2: 総合テスト仕様策定 (System Testing Plan)
承認済みの `SW105` **のみ**を入力として、共通レビュー手順を以下のパラメータで実行する。

| パラメータ | 値 |
| :--- | :--- |
| 作成スキル | `.agents/skills/test-engineer/SKILL.md` |
| レビュースキル | `.agents/skills/test-reviewer/SKILL.md` |
| 成果文書 | `docs/artifacts/SWP6_ソフトウェア総合テスト仕様書・報告書.md` |
| レビュー記録 | `docs/artifacts/REV101_Test.md` |

- **【要求へのループバック】**: テスト項目を書けない要求（検証条件が曖昧・矛盾・不足）を検出した場合、テスト仕様を推測で埋めずに、**Phase 1へ差し戻す**（`test-engineer` スキルの差し戻し手順に従う）。これがこのPhaseを設計より先に置く最大の目的である。

**完了条件**: すべての `REQ-ID` に対応する `TC-ID` が存在し（1:1以上）、Product Ownerの承認を得ていること。

## Phase 3: アーキテクチャ設計 (Architecture Design)
承認済みの `SW105` と `SWP6` を入力として、共通レビュー手順を以下のパラメータで実行する。

| パラメータ | 値 |
| :--- | :--- |
| 作成スキル | `.agents/skills/architect/SKILL.md` |
| レビュースキル | `.agents/skills/architecture-reviewer/SKILL.md` |
| 成果文書 | `docs/artifacts/SW205_ソフトウェアアーキテクチャ設計書.md` |
| レビュー記録 | `docs/artifacts/REV101_Design.md` |

**完了条件**: すべての `REQ-ID` がいずれかの `UNIT-ID` に対応付けられ、SWP6の「自動」テストが実行可能な構造であり、Product Ownerの承認を得ていること。

## Phase 4: 実装・デバッグ・報告 (Implementation & Testing)
1. **[自律実装]**: スキル `.agents/skills/programmer/SKILL.md` を読み込んでペルソナを適用。`unit-test-generator` スキルを活用し自律的にデバッグを行う。
2. **[バグ対応]**: バグを修正した際は、必ず `docs/artifacts/BUG101_バグ管理表.md` に記録し、解決済にすること。
   - **【設計へのループバック】**: バグ修正に伴い、アーキテクチャ設計（SW205）の変更が必要と判断した場合は、独断で実装を修正せず、必ず「Phase 3: アーキテクチャ設計」に戻り、設計書の修正と承認からやり直すこと。
3. **[レビュー]**: 成果物の完成後、報告の前にスキル `.agents/skills/code-reviewer/SKILL.md` を読み込んで自己検閲（Self-Correction）を行う。コーディング規約への準拠確認や品質評価を行い、必要なら再度プログラマーとして修正を行う。
4. **[完了報告]**: オールグリーン達成後、以下の定型でProduct Ownerに報告する。承認後、スキル `context-reset` を実行し、Phase 5へ遷移する。

   > Phase 4（実装）が完了しました。
   > - 実装したユニット: [UNIT-ID一覧]
   > - ユニットテスト: 全N件 グリーン
   > - 対応したバグ: [B-ID一覧 / なし]
   > - 起動・動作確認の手順: [コマンド等]

**完了条件**: 全ユニットテストがグリーンで、コードレビューのHigh指摘がゼロ（または残課題として報告済み）であること。

## Phase 5: 人間による評価と要求見直し (Human Evaluation & Requirement Review)
1. **[評価1: テストの確認とバグ対応]**: Product Owner（人間）がプログラムを実際に実行して評価する。（推奨ガイド: `docs/process/phase5_evaluation_guide.md`）。AIが自動テストできない領域のテスト結果は `SWP6` に人間が追記する。また、人間がバグを発見して `docs/artifacts/BUG101_バグ管理表.md` に記載した場合、AIはそれを読み取ってPhase 4に戻りデバッグを行い、修正後に `BUG101` に「原因と修正内容」を記入（解決済に）すること。
2. **[評価2: 要求仕様の変更検討]**: 実際のプログラムの動作を踏まえて要求仕様を変更したくなった場合、プロセスは**「Phase 1: 要求定義」にループバック**する。Product OwnerがAIに新しい要求を自然言語で指示するか、Product Owner自身が直接要求仕様書(`SW105`)を書き直す。
3. **[テスト仕様・設計への波及]**: 要求仕様が変更・確認された後は、自動的に**「Phase 2: 総合テスト仕様策定」へ遷移してSWP6を改訂**する。続いて、設計変更の要否をスケール判定し、必要なら「Phase 3: アーキテクチャ設計」を、不要なら直接「Phase 4: 実装」を実行する。
4. **[完了]**: 追加の要求変更がなく、Product Ownerの最終承認が得られた段階で、開発工程を完全に終了する。
