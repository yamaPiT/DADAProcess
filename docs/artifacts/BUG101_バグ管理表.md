# (BUG101) バグ管理表

【運用ルール】
1. **人間（Product Owner）**が、プログラムの動作確認やテスト（Phase 5等）で発見したバグを「期待値と実際の結果」列等に記入します。
2. **AIエージェント**は、この管理表を読み込んでソースコードをデバッグ・修正し、「原因と修正内容」を追記して、ステータスを「解決済」に変更します。

| バグID | 発生日 | 発生機能・発生条件 | 期待値と実際の結果 (人間が記入) | 原因と修正内容 (AIが記入) | ステータス |
| :--- | :--- | :--- | :--- | :--- | :--- |
| B001 | 2026-09-04 | UNIT-003: tools/verify_minutes.py / Windows環境でのパス判定 | Windows環境でパス区切り文字に `\` が使用された場合に入力形式不正と誤判定される懸念があった。期待値は `\` と `/` のいずれの区切り文字でも正しく `docs/<FileName>.txt` として解釈されること。 | `validate_input_path` 内で `file_path.replace("\\", "/")` を適用してパス文字列を正規化し、OS依存のない判定を行うよう実装した。（修正: `tools/verify_minutes.py`） | 解決済 |
